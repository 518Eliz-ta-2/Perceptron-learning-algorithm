# Perceptron-learning-algorithm
A simple implementation of the perceptron learning algorithm for binary classification. The model updates weights and bias based on misclassified samples until it correctly separates the data or reaches a set number of iterations.
